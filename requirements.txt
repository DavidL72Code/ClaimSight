# Deployment requirements. Use Python 3.10+; several security-fixed package versions require it.
fastapi==0.128.8
starlette==0.49.3
uvicorn[standard]==0.35.0
jinja2==3.1.6
python-multipart==0.0.27
pydantic==2.11.7
google-genai==1.30.0
firebase-admin==6.8.0
# Verifies Supabase access tokens. Currently arrives via firebase-admin as
# well, but this is a direct dependency now and must outlive it.
PyJWT==2.13.0
pillow==11.3.0
numpy==2.2.6
requests==2.33.0
urllib3==2.7.0
python-dotenv==1.2.2
filelock==3.20.3
# Optional MobileSAM (ONNX, CPU) mask refiner — only active when ENABLE_SAM2_ONNX=1
onnxruntime==1.23.2
huggingface_hub==0.25.2
